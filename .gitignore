.idea
.vscode
dist
node_modules

*.local

# Playwright (real-browser a11y tests) outputs
test-results
playwright-report
blob-report
playwright/.cache
# Agent worktrees (git worktrees created inside the repo by tooling)
.claude/worktrees/

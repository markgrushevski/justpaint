# Normalize line endings: store LF in the repo and check out LF everywhere
# (no CRLF churn on Windows).
* text=auto eol=lf

# Binary assets — never normalized.
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.woff  binary
*.woff2 binary
